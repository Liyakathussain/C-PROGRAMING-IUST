//area of rectangle
#include<stdio.h>
int main()
{
    int length,breadth,area;
    printf("enter the length");
    scanf("%d",&length);
    printf("enter the breadth\'");
    scanf("%d",&breadth);
    area=length*breadth;
    printf("area is %d",area);
    return 0;
}