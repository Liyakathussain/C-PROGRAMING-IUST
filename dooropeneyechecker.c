# include<stdio.h>

int main(){
    char D;
    printf("the D is %c",D);
    int i;
    char eyescan;
    if( i==1){
        printf("the eyescan is valid open the door");
    }
    elseif(i>=3);
        printf("the eyescan is not valid don't open the door");
    printf("work according condition is %c",D);
    

    return 0;
}